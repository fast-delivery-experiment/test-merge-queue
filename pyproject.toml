[project]
name = "test-merge-queue"
version = "0.1.0"
description = "Demo repository for GitHub Merge Queue"
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
